import styles from "../App.module.css";

export default function Submit() {
  return (
    <div className={styles.submitWrap}>
      <button className={styles.submitBtn}>Hai terminato? Consegna!</button>
    </div>
  );
}
